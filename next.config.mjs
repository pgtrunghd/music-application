/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "uvlsvqlnbnaspcqpuglf.supabase.co",
      },
    ],
  },
};

export default nextConfig;
